CREATE TABLE IF NOT EXISTS "Usuarios" ("id"   SERIAL , "usuario" VARCHAR(255) NOT NULL UNIQUE, "senha" VARCHAR(255) NOT NULL, "createdAt" TIMESTAMP WITH TIME ZONE NOT NULL, "updatedAt" TIMESTAMP WITH TIME ZONE NOT NULL, PRIMARY KEY ("id"));